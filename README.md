# 🚀 Hysteria 2 全平台通用一键管理脚本 (Universal Edition)

![Platform](https://img.shields.io/badge/OS-Alpine%20|%20Debian%20|%20Ubuntu%20|%20CentOS-blue?logo=linux)
![Hysteria 2](https://img.shields.io/badge/Service-Hysteria%202-orange)
![License](https://img.shields.io/badge/License-MIT-green)

这是一个极致轻量且功能强大的 Hysteria 2 一键安装脚本，完美适配 **Systemd** (Debian/Ubuntu/CentOS) 和 **OpenRC** (Alpine Linux) 两大系统架构。

---

## ✨ 核心特性

- **🌍 全系统适配**：自动识别 Alpine, Debian, Ubuntu, CentOS, Rocky, AlmaLinux, Fedora。
- **🚀 交互式菜单**：安装完成后，只需输入 `hy2` 即可调出全功能管理面板。
- **📦 架构自识别**：智能匹配 x86_64、AARCH64 (ARM64) 等硬件架构。
- **⚡ 性能调优**：内置一键开启内核 BBR 加速，针对 LXC 容器环境提供专门的 UDP 优化方案。
- **🔐 自动化安全**：全自动生成 ECC 自签名证书，自动处理 Systemd 权限及用户组安全。
- **🔗 一键分享**：自动生成 `hysteria2://` 分享链接，支持快速导入客户端。

---

## 🚀 快速开始

在你的服务器终端执行以下命令：

```bash
wget --no-check-certificate -O install.sh [https://raw.githubusercontent.com/LoganLazy/Hysteria-2_easy/main/install.sh](https://raw.githubusercontent.com/LoganLazy/Hysteria-2_easy/main/install.sh) && chmod +x install.sh && ./install.sh
```

### 快捷指令

安装完成后，随时输入以下命令管理您的 Hy2 服务：

```
hy2
```

## 📸 面板预览

运行 `hy2` 后的可视化菜单：

```
==============================================
    Hysteria 2 全平台管理脚本 (V5.0)    
==============================================
 系统: alpine/debian/centos  架构: x86_64
 状态: 运行中
----------------------------------------------
 1. 安装 Hysteria 2
 2. 查看配置信息 & 链接
 3. 启动服务      4. 停止服务
 5. 重启服务      6. 开启内核 BBR 加速
 7. 卸载 Hysteria 8. 查看实时日志
 0. 退出脚本
----------------------------------------------
```

## ⚠️ 避坑指南

### 1. 防火墙设置 (CentOS/Ubuntu)

如果安装后状态正常但无法连接，请务必检查并开放 UDP 端口：

Bash

```
# CentOS 示例 (放行 443 端口)
firewall-cmd --zone=public --add-port=443/udp --permanent
firewall-cmd --reload
```

### 2. LXC 虚拟化优化

如果你在 LXC 容器中运行，建议在 **宿主机** 执行以下命令以获取最佳 UDP 性能：

Bash

```
sysctl -w net.core.rmem_max=16777216
sysctl -w net.core.wmem_max=16777216
```

### 3. 客户端配置

由于使用的是自签名证书，请确保客户端：

- 开启 **"允许不安全连接" (Insecure/AllowInsecure: true)**。
- SNI 设置为 **`bing.com`**。
